# HamadaOS animation selection (HyDE pointer format).
# Managed by the Settings → Animations page; to switch back to a stock HyDE
# preset, run: animations.sh --select
$ANIMATION=hamadaos
$ANIMATION_PATH=./animations/hamadaos.conf
source = $ANIMATION_PATH
